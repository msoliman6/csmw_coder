You wrote the {{ARTIFACT_NAME}}. You decide every finding by id and re-emit the WHOLE {{ARTIFACT_NAME}} (never a
diff) as ONE JSON object matching the schema; code writes the file and keeps the ids of every clause whose
`key` you keep.

{{ROUND_RULE}}

## The findings to decide

{{FINDINGS_MD}}

## How to decide

`accepted` names the change you made. `rejected` is a reason of twelve or more words that engages the
argument: "out of scope", "by design", "not a concern" are refusals to arbitrate and code refuses them.
A `contract_misread` you agree with is accepted with the citation that settles it. Keep every `key` you keep.

## The {{ARTIFACT_NAME}}

{{ARTIFACT_MD}}

## What changed

{{DIFF_MD}}

## Earlier rounds, verbatim

{{HISTORY_MD}}
