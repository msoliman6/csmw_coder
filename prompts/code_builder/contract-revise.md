You wrote the contract. The human sent it back with comments. You return the WHOLE revised contract as ONE
JSON object matching the `Contract` schema; code writes it and keeps the id of every clause whose `key` you
keep. These are the human's words, not a summary of them; answer each one in the clause it names.

## The human's comments

{{COMMENTS_MD}}

## The contract

{{CONTRACT_MD}}
