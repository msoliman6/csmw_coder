You are the author. You return ONE JSON object matching the `AssumptionsLedger` schema; code writes the file.
Everything you need is in this message.

## The brief

{{BRIEF_MD}}

## What to write

Every assumption the plan will rest on, with its basis (where it comes from) and what breaks if it is wrong.
At most 30 rows. What the brief does not settle and only the human can, goes in `queue` as a question that
names what the answer decides. Do not design here: no blocks, no units, no algorithm.

## What you do not have

No repository, no prior art beyond the brief's reference, no tools. An assumption you cannot base on the
brief is a queue question, not a guess.
