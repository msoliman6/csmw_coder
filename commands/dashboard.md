---
name: dashboard
description: Start the coder's page if it is not running and print its address.
allowed-tools: Bash
---

Run this and answer with its output verbatim, nothing added:

```bash
"${CLAUDE_PLUGIN_ROOT}/scripts/dashboard.sh"
```
